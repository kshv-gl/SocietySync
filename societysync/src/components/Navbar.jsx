function Navbar() {
  return (
    <nav className="navbar">
      <h2>SocietySync</h2>

      <div className="nav-buttons">
        <button>Login</button>
        <button>Signup</button>
      </div>
    </nav>
  );
}

export default Navbar;